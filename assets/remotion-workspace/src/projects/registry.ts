import type {LivePhotoProject} from "./types";
import {project as starterProject} from "./_template";

// Replace starterProject with real project imports after first initialization.
export const projects: LivePhotoProject[] = [starterProject];
